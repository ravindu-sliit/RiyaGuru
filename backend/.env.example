# MongoDB connection string (required)
MONGO_URI=mongodb://localhost:27017/riyaguru

# JWT signing secret (optional but recommended)
JWT_SECRET=your_jwt_secret_here

# Email settings used by nodemailer (Gmail example)
EMAIL_USER=your.email@gmail.com
EMAIL_PASS=your-app-password-or-smtp-password

# Optional: PORT
PORT=5000
